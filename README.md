# 北京交通大学食堂就餐仿真平台

本项目是一个基于 Flask + SimPy 的食堂就餐仿真原型系统。

目录说明：
- `app.py`：Flask 服务入口。
- `cafeteria_sim/`：仿真核心代码。
- `templates/`：页面模板。
- `static/`：样式和前端脚本。

运行思路：
1. 在启动页输入窗口数、桌数、仿真时长等参数。
2. 后端执行一次完整仿真，并返回时间线数据。
3. 前端在仿真页按时间线播放动态过程。
4. 用户进入结果页查看汇总指标和图表。
